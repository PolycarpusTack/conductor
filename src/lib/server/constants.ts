/** Maximum character length for agent output, step output, and event payloads. */
export const MAX_OUTPUT_CHARS = 5000

/** Grace period (ms) after SIGTERM before escalating to SIGKILL. */
export const KILL_GRACE_MS = 5000
